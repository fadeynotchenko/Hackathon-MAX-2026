"""HTTP-слой мини-аппа (FastAPI). Тонкий: парсит вход → зовёт сервис → рендерит ответ.

Бизнес-логики здесь нет — она в core.usecases. Доступ к БД — только через
репозитории внутри сценариев. Всё, что нужно и API, и джобам, живёт в
core.usecases, core.events или core.domain, а не импортируется отсюда.
"""
